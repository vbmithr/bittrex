(executables
 ((names (bittrex_data))
  (public_names (bittrex-data))
  (modules (bittrex_data))
  (preprocess (pps (ppx_sexp_conv)))
  (libraries (bitv bs-devkit leveldb tick dtc-pb bittrex-async))))

(executables
 ((names (bittrex_data bittrex))
  (public_names (bittrex-data bittrex))
  (modules (bittrex_data bittrex))
  (preprocess (pps (ppx_sexp_conv)))
  (libraries (bitv bs-devkit leveldb tick dtc-pb btrex btrex-async))))
